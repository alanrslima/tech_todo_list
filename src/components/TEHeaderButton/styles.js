import { StyleSheet } from 'react-native';
import { metrics } from '~/styles';

export default StyleSheet.create({
  container: {
    height: metrics.HEADER_BUTTON,
    width: metrics.HEADER_BUTTON,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
